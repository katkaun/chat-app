// const Avatar = () => {
//     const avatarUrl = auth.avatar || 'default-avatar-url'; // Use default if auth.avatar is empty
  
//     return (
//       <img src={`${avatarUrl}?${new Date().getTime()}`} alt="User Avatar" />
//     );
//   };

//   export default Avatar;